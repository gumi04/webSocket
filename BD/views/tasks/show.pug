doctype html 
html
  head
    title Tareas
  body
    h1= tarea.description
    p= tarea.id
    form(action=`/tasks/${tarea.id}?_method=DELETE` method="POST")
      input(type="submit" value="Eliminar") 
